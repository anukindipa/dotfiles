<!-- return {
    "folke/which-key.nvim",
    opts = {},
    keys = {
        {
            "<leader>?",
            function()
                require("which-key").show("<leader>", { mode = "n" })
            end,
            desc = "Show <leader> keymaps (which-key)",
        },
    },
} -->
